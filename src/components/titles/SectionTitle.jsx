const SectionTitle = ({title}) => {
    return (
        <div className={`sec-tlt`} >
            <h2> {title} </h2>
            <h3> {title} </h3>
        </div>
    );
};

export default SectionTitle;
